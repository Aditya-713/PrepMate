# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb://127.0.0.1:27017/prepmate

# Authentication
JWT_SECRET=prepmate_super_secret_jwt_key_2026_change_in_production
JWT_EXPIRES_IN=7d

# AI Service Keys (Optional - Smart Mock Fallback Active if Keys Omitted)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
DEFAULT_AI_PROVIDER=mock

# Admin Initial Passkey (used to claim admin role on signup if needed)
ADMIN_SIGNUP_CODE=admin123secret
